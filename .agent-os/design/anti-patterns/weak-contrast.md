# Weak Contrast

Problem:
- Text or interactive elements fail contrast requirements.

Why it hurts:
- Accessibility failure and unreadable UI.

Detection signals:
- User hesitation, low task completion, or inconsistent visual rhythm.

Fix:
- Increase contrast and avoid using color alone for meaning.

Do instead:
- Use design principles and system tokens first.
