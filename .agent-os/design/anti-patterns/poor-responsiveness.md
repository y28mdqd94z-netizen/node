# Poor Responsiveness

Problem:
- Layouts that only shrink instead of adapting to device context.

Why it hurts:
- Mobile usability failures and hidden actions.

Detection signals:
- User hesitation, low task completion, or inconsistent visual rhythm.

Fix:
- Adapt structure per breakpoint; keep core actions visible.

Do instead:
- Use design principles and system tokens first.
